def find_pair_with_sum(lst):
    seen = {}
    result = []
    for num in lst:
        complement = 1 - num
        if complement in seen:
            result.append([complement, num])
        seen[num] = True
    return result
